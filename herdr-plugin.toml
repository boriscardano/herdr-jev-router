id = "herdr.jev-router"
name = "Herdr Jev Router"
version = "0.1.4"
min_herdr_version = "0.9.1"
description = "Advisory Jev routing for stock Herdr child-agent spawns."
platforms = ["linux", "macos"]

[[build]]
command = ["uv", "tool", "install", "--force", "."]
